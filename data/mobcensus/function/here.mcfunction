# Monster-category mobs (#mobcensus:cap_mobs) within the configured radius of
# you - every loaded one, persistent included (unlike the cap commands which
# filter to non-persistent). Click a result to teleport. Cost: one entity scan,
# plus one line per mob in range.
function mobcensus:_here with storage mobcensus:config
